{
  "name": "plugin-server",
  "version": "1.0.0",
  "main": "dist/server.ts",
  "license": "MIT",
  "dependencies": {
    "koa": "^2.14.2",
    "koa-body": "^6.0.1"
  },
  "devDependencies": {
    "@types/koa": "^2.13.6",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts"
  }
}
